# API Configuration
# Leave empty to use the proxy (recommended for development)
# The Vite dev server proxies /api/* to http://localhost:3000
VITE_API_URL=

# For production, set the full API URL:
# VITE_API_URL=https://api.your-domain.com/api/v1
