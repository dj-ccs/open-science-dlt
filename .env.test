# Test Environment Configuration

NODE_ENV=test

# Test Database (Use separate test database!)
DATABASE_URL=postgresql://user:password@localhost:5432/open_science_dlt_test?schema=public

# JWT Secrets (Test values)
JWT_SECRET=test-jwt-secret-key-for-testing-only
REFRESH_TOKEN_SECRET=test-refresh-token-secret-for-testing-only

# API Configuration
API_HOST=127.0.0.1
API_PORT=3001
CORS_ORIGINS=http://localhost:3001

# Stellar (Testnet)
STELLAR_NETWORK=testnet

# IPFS (Testnet)
IPFS_NODE_URL=https://ipfs.infura.io:5001
IPFS_GATEWAY_URL=https://ipfs.io/ipfs

# Logging
LOG_LEVEL=error
