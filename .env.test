# Test Environment Configuration

NODE_ENV=test

# Test Database - Supabase PostgreSQL
# CRITICAL: Use quotes and port 5432 (direct connection) for migrations and tests
DATABASE_URL="postgresql://postgres.dlaedexiqhjigoojutdx:TJ1R7nyqKLAIrmSS@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres"

# JWT Secrets (Test values)
JWT_SECRET=test-jwt-secret-key-for-testing-only
REFRESH_TOKEN_SECRET=test-refresh-token-secret-for-testing-only

# API Configuration
API_HOST=127.0.0.1
API_PORT=3001
CORS_ORIGINS=http://localhost:3001

# Stellar (Testnet)
STELLAR_NETWORK=testnet

# IPFS (Testnet)
IPFS_NODE_URL=https://ipfs.infura.io:5001
IPFS_GATEWAY_URL=https://ipfs.io/ipfs

# Logging
LOG_LEVEL=error
